import Link from "next/link";
import { notFound, redirect } from "next/navigation";

import { prisma } from "@/lib/prisma";
import { requireOrgSession } from "@/lib/org-context";
import { can } from "@/lib/permissions";
import { approveStockCountAction, cancelStockCountAction } from "../actions";

export const dynamic = "force-dynamic";

export default async function StockCountDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const { user, orgId } = await requireOrgSession();
  if (!can.manageInventory(user)) redirect("/inventory");
  const count = await prisma.stockCount.findUnique({ where: { id }, include: { location: { select: { name: true, code: true } }, createdBy: { select: { name: true, email: true } }, approvedBy: { select: { name: true, email: true } }, items: { include: { part: { select: { sku: true, name: true } } }, orderBy: { createdAt: "asc" } } } }).catch(() => null);
  if (!count || count.orgId !== orgId) notFound();
  const fmt = (d: Date | null) => d ? d.toLocaleDateString("en-UG", { day: "numeric", month: "short", year: "numeric" }) : "-";
  const varianceLines = count.items.filter((item) => item.varianceQty !== 0).length;

  return <div className="max-w-4xl space-y-6"><div className="flex items-start justify-between gap-4"><div><Link href="/inventory/stock-counts" className="text-xs font-semibold text-[var(--ink-muted)] hover:text-[var(--ink)]">← Stock Counts</Link><h1 className="mt-1 text-xl font-bold text-[var(--ink)]">{count.countNumber}</h1><p className="mt-0.5 text-sm text-[var(--ink-muted)]">{count.location.name}{count.location.code ? ` (${count.location.code})` : ""}</p></div><span className="mt-1 rounded-full bg-sky-100 px-3 py-1 text-xs font-semibold text-sky-700">{count.status}</span></div><div className="grid grid-cols-2 sm:grid-cols-4 gap-3"><div className="rounded-lg border border-[var(--line)] bg-[var(--panel)] px-3 py-2"><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-[var(--ink-muted)]">Counted</p><p className="mt-0.5 text-sm font-semibold text-[var(--ink)]">{fmt(count.countedAt)}</p></div><div className="rounded-lg border border-[var(--line)] bg-[var(--panel)] px-3 py-2"><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-[var(--ink-muted)]">Items</p><p className="mt-0.5 text-sm font-semibold text-[var(--ink)]">{count.items.length}</p></div><div className="rounded-lg border border-[var(--line)] bg-[var(--panel)] px-3 py-2"><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-[var(--ink-muted)]">Variances</p><p className="mt-0.5 text-sm font-semibold text-[var(--ink)]">{varianceLines}</p></div><div className="rounded-lg border border-[var(--line)] bg-[var(--panel)] px-3 py-2"><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-[var(--ink-muted)]">Approved</p><p className="mt-0.5 text-sm font-semibold text-[var(--ink)]">{fmt(count.approvedAt)}</p></div></div><div className="rounded-xl border border-[var(--line)] bg-[var(--panel)] overflow-hidden"><table className="w-full text-sm"><thead><tr className="border-b border-[var(--line)] text-xs font-semibold uppercase tracking-wide text-[var(--ink-muted)]"><th className="px-4 py-2 text-left">Part</th><th className="px-4 py-2 text-right">System</th><th className="px-4 py-2 text-right">Counted</th><th className="px-4 py-2 text-right">Variance</th><th className="px-4 py-2 text-left hidden sm:table-cell">Note</th></tr></thead><tbody className="divide-y divide-[var(--line)]">{count.items.map((item) => <tr key={item.id} className={item.varianceQty !== 0 ? "bg-amber-50/40" : ""}><td className="px-4 py-2"><p className="font-semibold text-[var(--ink)]">{item.part.sku}</p><p className="text-xs text-[var(--ink-muted)]">{item.part.name}</p></td><td className="px-4 py-2 text-right tabular-nums text-[var(--ink-muted)]">{item.systemQty}</td><td className="px-4 py-2 text-right tabular-nums text-[var(--ink)]">{item.countedQty}</td><td className="px-4 py-2 text-right font-semibold tabular-nums text-[var(--ink)]">{item.varianceQty}</td><td className="px-4 py-2 hidden sm:table-cell text-xs text-[var(--ink-muted)]">{item.note ?? "-"}</td></tr>)}</tbody></table></div>{count.note ? <div className="rounded-xl border border-[var(--line)] bg-[var(--panel)] px-5 py-4 text-sm text-[var(--ink)] whitespace-pre-wrap">{count.note}</div> : null}<div className="flex flex-wrap items-center justify-between gap-3 text-xs text-[var(--ink-muted)]"><p>Created by {count.createdBy.name || count.createdBy.email}.</p>{count.status === "SUBMITTED" ? <div className="flex gap-2"><form action={approveStockCountAction}><input type="hidden" name="id" value={count.id} /><button className="rounded-lg bg-emerald-500/15 px-3 py-2 font-semibold text-emerald-700">Approve and adjust stock</button></form><form action={cancelStockCountAction}><input type="hidden" name="id" value={count.id} /><button className="rounded-lg border border-[var(--line)] px-3 py-2 font-semibold text-red-600">Cancel</button></form></div> : null}</div></div>;
}
