services:
  - type: web
    name: mrms
    runtime: node
    plan: starter
    buildCommand: bun install --frozen-lockfile && bunx prisma generate && bun run build
    startCommand: bunx prisma migrate deploy && ALLOW_EMPTY_DB_RECONCILE=1 bun run db:reconcile-empty && bun run start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NEXT_PUBLIC_APP_URL
        sync: false
      - key: BETTER_AUTH_URL
        sync: false
      - key: BETTER_AUTH_SECRET
        sync: false
      - key: DATABASE_URL
        value: file:/var/data/dev.db
      - key: ALLOW_SQLITE_PRODUCTION
        value: "1"
      - key: UPLOADS_DIR
        value: /var/data/uploads
    disk:
      name: mrms-data
      mountPath: /var/data
      sizeGB: 5
