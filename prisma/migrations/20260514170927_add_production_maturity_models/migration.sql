-- This migration was applied directly to the production database before local tracking began.
-- Placeholder ensures prisma migrate deploy does not report a diverged history.
-- No SQL needed: schema changes from this migration are superseded by subsequent migrations.
SELECT 1;
